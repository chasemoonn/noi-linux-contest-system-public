# 安全策略

Hydro NOI Contest Kit 涉及云资源、学生桌面、比赛凭据、程序回收和 Hydro 记录。请不要在公开 Issue、PR、日志附件或聊天记录中提交以下信息：

- `.env`、`config.yaml`、AccessKey、SSH 私钥、Token、管理员密码；
- 学生桌面链接、gateway token、VNC 密码、准考证号；
- 真实 EIP、实例 ID、安全组 ID、主机指纹和内部拓扑；
- 隐藏测试数据、标准答案、学生源码、收卷目录和数据库备份；
- 未脱敏的 Caddy、Hydro、MongoDB、Docker 或云 API 日志。

## 报告漏洞

在建立专用安全邮箱前，请通过仓库维护者 GitHub 主页提供的私下联系方式报告漏洞。报告中请包含：

1. 受影响的 Git tag、commit 或镜像 digest；
2. 漏洞类型和可观察影响；
3. 脱敏后的最小复现步骤；
4. 是否需要活动比赛、学生凭据或管理员权限；
5. 建议的临时缓解措施。

不要为了验证漏洞而访问他人的比赛、桌面、账号、源码或云资源。

## 支持范围

安全修复优先覆盖最新开发分支和最近一个明确标记的发布版本。Alpha 阶段尚未承诺长期支持周期；每个 Release Notes 会列出兼容版本和升级要求。

## 默认安全边界

- 所有秘密必须放在权限受限的文件或密钥服务中，不进入 Git、命令行历史或日志。
- SSH 必须校验明确的主机密钥指纹，禁止 `AutoAddPolicy` 式信任。
- 学生入口只在唯一、未截止、已经完整验收的 `ready` 比赛中开放。
- 活动比赛期间禁止升级控制面、插件和桌面镜像。
- 安全组、通知、截止计时器、Caddy 或收卷状态无法证明正确时，系统必须失败关闭。
- 普通 Hydro OJ 首页和登录是任何安装、升级和回滚的硬门禁。
